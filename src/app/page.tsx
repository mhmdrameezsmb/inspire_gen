// Assuming 'inspire' is a React component, not an HTML element
import Inspire from './inspire/page'; // Assuming 'Inspire' is the correct component name

export default function Home() {
  return (
    <>
      <Inspire />
    </>
  );
}
